# 00 · 当前状态

> ★ **新对话从这里开始。** 读本文件 + [`AGENT.md`](../AGENT.md) 即可接上进度，**不需要聊天记录**。
>
> 这是**唯一会频繁变化**的文档。其余文档都是稳定的设计资料，只在设计真正变化时才改。

**最后更新**：2026-09-12

---

## 现在在哪

**阶段：程序已经能日常使用；界面重做基本完成，剩下的是搜索/过滤这块硬骨头。**

```
线 A：新前端        ✅0 ✅1 ✅2 ✅3 ✅4 ✅5 ✅6   ← 全部完成
线 B：后端服务化    ✅B1 ✅B2 ✅B3 ✅B4 ✅B5 ✅B6   ← HTTP + WebSocket，WebView2 已彻底移除
线 C：界面迁移      ✅C1 ✅C3（设置 / 数据库） ▶ C2 过滤面板：**后端已就绪，前端未做** · C4 清理 ⬜
```

---

## ★ 当前形态

```
启动 IAGrim.exe
  → 主窗口**永不显示**（SetVisibleCore 拦住，不是"显示后再 Hide"）
  → 托盘常驻（双击 / 右键 Open → 打开浏览器）
  → 自动用系统浏览器打开 http://127.0.0.1:3031/
  → 界面是 React 前端，跑在 C# 自带的 Kestrel 上
```

| 事实 | 说明 |
|---|---|
| 前端 | `WebUI-next/`（React + TS + Vite），产物放 `%LOCALAPPDATA%\EvilSoft\IAGD\storage\` |
| 旧前端 `WebUI/` | Preact 版，已被取代（目录还在，只作参考） |
| 后端 | C# 内的 Kestrel，`127.0.0.1:3031`（见 `04-开发环境.md` §9） |
| 推送 | WebSocket `/ws`，只发轻量信号 |
| WebView2 | ❌ **已彻底移除**（代码 + NuGet 包引用都没了，构建输出 41 个文件） |
| WinForms 主界面 | ❌ **已全部移除**；`MainWindow` 只剩不可见宿主（消息循环 + `Invoke`） |
| `tools/devapi` | ⚠️ **已退役**，留着仅供对照 |
| `IAGrim/UI/Tabs/` | 只剩一个 `Util` 目录（所有标签页都删了） |

⚠️ 仍有若干**按需弹出的 WinForms 对话框**（不是常驻界面）：
`StashPicker`（`TransferAnyMod` 开启时选目标 mod）、`StashTabPicker`、
`AddEditBuddy`、`UpdateModal`、`DownloadingUpdateModal`、`DonateNagScreen`、
`NumericFilterDialog`、`LanguagePackPicker`。
另有 `ImportExport/`（导入导出**物品**）**已无入口** —— 见下方「数据安全」。

施工过程、坑与教训 → [`10-界面解耦.md`](./10-界面解耦.md)。

### ⚠️⚠️ 一个必读的坑：原版 IA 会覆盖 storage

`C:\Program Files\IAGD\IAGrim.exe`（原版）与我们的构建**共用同一个用户数据目录**
（`%LOCALAPPDATA%\EvilSoft\IAGD\`），而原版启动时会把它自带的**旧前端**解压覆盖
进 `storage/`。**一打开原版，浏览器界面就会变回老样式。**

已做自愈：csproj 把 `WebUI-next/build` 拷进输出目录的 `webui/`，
新增 `Services/FrontendDeployer.cs` 在启动时比较并重新铺一遍
（**只碰前端文件，绝不动 4573 个物品图标**）。

---

## 几条关键事实（都实测过）

### 转移通路（双向通）

`POST /api/items/transfer`，请求体 `{ ids: number[], transferAll: boolean }`，
`ids` 是 `PlayerItem.Id`。响应 `{ success, numTransferred, failed }`。

- C# 把 id 拼成 `["PI", id, "", "", "", false]` 交给 `ItemTransferController`，
  与旧 WebView2 路径**同一个控制器**。
- 它把物品序列化成 CSV 写进 `CsvLocationOutgoing`（**不是直接写存档**），
  由注入游戏的 hook 读取；然后把 `StackCount` 置 0 由 `Update` 删记录。
- ⚠️ **失败时也返回 HTTP 200**（`{success:false, error:"…"}`）。前端必须判
  `success` 字段，只看 `res.ok` 会把"转移失败"显示成"已转移 0 件"。
- ⚠️ 「转移到任意 Mod」会让控制器弹 `StashPicker` 模态框（必须 UI 线程），
  所以**开启该设置时 HTTP 转移直接返回明确错误**，不会半路崩。

### 列表自动刷新（B2）

`ws://127.0.0.1:3031/ws`，服务端 → 客户端单向。数据库一变就广播：

| 消息 | 触发 |
|---|---|
| `itemsChanged` | 拾取 / 转移 / 重新解析 / 改 `hideSkills` |
| `maintenance` | 维护任务的进度（含百分比与阶段名） |
| `notification` | 后端主动提示（对应 C# 的 `IUserFeedbackHandler`） |

- ★ **只发轻量信号，数据仍走 REST** —— 不沿用旧的 `IOMessage` 枚举
  （那套是喂旧 Preact 前端的，新前端从没消费过）。理由见 `03-目标架构.md` §4.3。
- 广播点：`CsvParsingService.OnItemLooted`（**拾取走的是这个**）、
  `ListviewUpdateTrigger`、`DatabaseLoadedTrigger`、HTTP 转移成功后。
- 前端 `connectLive()` 自动重连；**断连期间回退到每 4 秒轮询**。
- ⚠️ 已知限制：改动**已有**物品（属性重算、改名）不会触发广播。

### 维护操作在网页上

加载游戏数据库 / 配置 / 更新项目统计三个操作，
进度通过 `maintenance` 消息推送，期间查询被拒（503）+ 整屏遮罩。

- 「清除数据库」**故意没搬**：它做的事「加载数据库」第一步就做了，
  却能制造一个"物品还在、但没名字没属性"的界面。后端端点保留但无入口。
- 配置因为浏览器**拿不到文件夹路径**，改成粘贴路径 + 后端校验。
- 细节 → [`11-备份与恢复.md`](./11-备份与恢复.md) 与 `05-实施计划.md` §6。

### 装备显示

- 详情渲染的是**游戏原样导出的 tooltip**（`replicaStats`，带 `^X` 颜色代码），
  不是从 `DatabaseItemStat` 重拼的（那覆盖不全，实测 `headerStats` 全是空的）。
- 物品名由**前端用内置词缀表组装**（`model/affixes.json`，503 条），
  不再依赖游戏当前汉化包。
- ★ **配色、缩进、职业名都对着游戏校准过**（2026-09-12）：tooltip 分三级缩进；
  伤害/抗性/词缀等各档颜色按游戏内实测值改；职业名按 10 个职业的主图各配一色
  （尖括号跟随职业色，技能名用属性数值那种黄）。
- ★ **名字的颜色也是问游戏**：基底名沿用游戏名字行给的颜色码
  （普通底材白 `^w`、MI 底材绿 `^l`），词缀颜色不外溢到基底名。
- 三种视图：分栏列表 / 简洁卡片 / **详细对照**（并排比较）。
- 布局：**只有物品列表滚动**，工具条不动；详情支持左/右固定栏。
- 细节与踩过的坑 → [`12-装备显示与交互.md`](./12-装备显示与交互.md)。

---

### 搜索与过滤（后端 2026-09-12 全部打通）

| 能力 | 状态 | 说明 |
|---|---|---|
| 名字搜索 | ✅ | `wildcard`，空格视为多个关键词（旧行为） |
| **按属性名搜索** | ✅ | 搜「火焰抗性」会找出**带该属性的物品**，不只是名字匹配 |
| **属性数值过滤** | ✅ | 「火焰抗性 ≥ 20」，走预计算表 `ComputedItemStat` |
| 槽位 / 品质 / 等级 / 职业 / 镶嵌 / 重复 / 战宠 | ✅ | 后端本来就有，`POST /api/search` 直接接受 |
| **`GET /api/filters/options`** | ✅ | 品质 / 槽位 / 职业 / 比较符 / 分组复选框 / 属性下拉 |
| 前端过滤面板（C2） | ⬜ | **只剩它了**，后端不再缺东西 |

- ★ **属性数值过滤不是新写的**：`PlayerItemDaoImpl` 早就实现了
  `Filters`（属性存在性）与 `StatValueFilters`（数值），只是新前端没接、也没有可选项接口。
- ★ **过滤器的定义在后端**（`Services/Filters/FilterCatalog.cs`）：
  照搬旧 WinForms 面板的 4 组（伤害 / 持续伤害 / 抗性 / 杂项），
  每个复选框对应哪些 stat 字段、哪些支持数值，都由后端说了算——
  前端不再抄一份映射，避免"勾了火焰抗性却按别的字段过滤"这种难查的错。
- ⚠️ **Minimal API 默认用 System.Text.Json，不接受字符串枚举**：
  `{"operator":"GreaterOrEqual"}` 会直接 **400 且响应体为空**。
  已在 `WebServer.Start()` 打开枚举名支持（只影响请求绑定，响应仍走 Newtonsoft）。
- ⚠️ 槽位清单**写死**在 `StatTranslator.SlotTranslator`，**不能**查库：
  `DatabaseItemStat_v2` 里 `stat='Class'` 实测有 **175** 个值，
  混着容器、可破坏物、怪物、药水等非装备类别。

---

## 新前端已有的页面

| 页签 | 状态 |
|---|---|
| **物品** | ✅ 三种视图、搜索、hover 预览 / 点击固定、左右固定栏、直接「取出」按钮；**两种加载方式**（无限滚动 / 翻页）可切换，每页（每批）可选 50 / 100 / 200 / 500 条 |
| **设置** | ✅ 两栏（设置项 / 动作）：隐藏技能、搜索延迟、转移到任意 Mod、额外备份目录、Stash Configuration；重置 / 导入 / 导出 / 查看备份 / 查看日志 |
| **数据库** | ✅ 加载数据库 / 配置 / 更新项目统计 + 进度 |
| ~~图鉴~~ | ❌ 已决策删除，前后端代码均已移除 |

---

## 下一步（按优先级）

| # | 任务 | 说明 | 在哪 |
|---|---|---|---|
| 1 | **C2 过滤面板**（前端，工作量最大） | ✅ **后端已就绪**：`GET /api/filters/options` 返回品质 / 槽位 / 职业 / 比较符 / 分组复选框 / 属性下拉；`POST /api/search` 已接受 `filters`、`statValueFilters`、`slot`、`classes` 等全部字段（2026-09-12 逐条实测）。界面按设计文档的信息架构做即可 | `05-实施计划.md` §4、`09-高级搜索.md` §3–§4 |
| 2 | **收尾清理**（低优先，不影响使用） | `MainWindow` 该改名 `AppHost`；Designer 里还留着空的 TabControl 与 Panel；`SearchController` 里旧界面专用方法已无人调用 | `10-界面解耦.md` §6 |

> C2 现在是**纯前端工作**：后端不再缺任何东西，`WebUI-next/src/api/types.ts` 里
> 已有完整且与后端一致的请求/响应类型。

---

## 待办

### ⚠️ 数据安全：备份恢复**没有入口**（优先于上面的清理项）

**IA 只自动备份，不自动恢复。** 备份一直在生成
（`%LOCALAPPDATA%\EvilSoft\IAGD\backup\<星期几>.zip`，含**角色存档 + `export.ias`**），
但能读回它们的那个窗口（`UI/Popups/ImportExport/`）随旧界面删除后**已无入口**。

细节、设置里那两项的真正含义、以及"浏览器能不能弹窗选文件"的结论，
全部写在 → [`11-备份与恢复.md`](./11-备份与恢复.md)。
使用者 2026-09-12 决定**先做属性搜索**，此项暂缓。

### 需要使用者决策 / 确认

- [ ] **「导入设置」要不要也做热重载**？目前它仍会**重启程序**——导入的是整个
      `settings.json`，而 `SettingsService._data` 是 `readonly`，没法整体替换
      （替换会让订阅了 `OnMutate` 的地方失效）。选项：(a) 保持重启（现状）；
      (b) 把导入的 JSON 逐字段写回现有对象。
- [ ] 定"关键属性"的判定规则（分栏列表要不要加列会用到）→ `03-目标架构.md` §6.2
- [x] ~~视觉设计方向（参考 grimtools 还是游戏原生风格）~~ → **已定（2026-09-12）：以游戏内实际观感为准、逐项校准**（色值对着游戏取，不搞"参考某站风格"；grimtools 只在具体实现上借鉴，如滚动条）→ `03-目标架构.md` §9

### 明确记录的功能打算

- [ ] **云备份（自建）**：现有「多电脑共用 / 在线备份」依赖**原作者的服务器**，
      所以先不做。将来要**做自己的云备份方式**。
- [ ] **Reset settings 的备份范围**：重置时备份的是**设置文件**
      （`settings.json` → `settings-backups/`），**不是物品数据**。别搞错。
- [ ] **语言锁死中文**：`languageCode` 不再暴露给前端。真要改语言的话，物品名
      不会立刻变——C# 需要**重新解析游戏数据**才生效。

### 代码 / 环境

- [ ] **属性翻译的特殊层**（技能修饰、伤害转换、种族加成）→ `08-属性翻译.md` §5.2
- [ ] **模板 key 有前缀变体**（如 `customtag_xpac_modif_skillManaCostReduction`），
      简单层只做同名直取，这类会漏 → `08-属性翻译.md` §5.2
- [ ] 确认物品是否有"占几格"的尺寸数据（决定将来能否做图标网格）→ `06-外部参考.md`
- [ ] 把 `npm run build` → 拷进 `storage` 写成脚本（目前手动；启动时的自愈已经
      覆盖了"被原版覆盖"的情形，但开发时仍需手动部署）
- [ ] ⚠️ **分发提醒**：Kestrel 需要 `Microsoft.AspNetCore.App` 共享框架；
      开发机随 .NET SDK 已有，**打包给别人时要带上** → `04-开发环境.md` §9.1
- [ ] `headerStats` 实测全为空，值得查是不是解析侧的问题（目前靠 `replicaStats` 兜住了）
- [ ] ⚠️ **后端 bug：`offset > 0` 时 `total` 返回的是"本页条数"**。
      `PlayerItemDaoImpl.SearchForItems` 里 `else if (!wasTruncated) totalCount = items.Count;`
      这个推断**只对 `skip == 0` 成立**，所以翻到第 2 页时总数会突然缩水
      （实测 66 件、每页 50：第 2 页返回 16 条，后端说 `total=16`，界面会显示"第 2 / 1 页"）。
      前端已规避——**只采信 `offset == 0` 那次的 total**（见 `App.tsx` 的 `fetchRange`），
      但根在后端。修法：那个分支补上 `skip == 0`，并让 `SearchController.QueryItems`
      在 `offset > 0` 时传 `computeCount: true`（只在翻页时才付一次 COUNT 的代价）

### 已完成（保留在此是为了避免重复讨论）

- [x] ~~线 A 全部~~、~~新前端上线（B4）~~、~~设置页~~、~~删除图鉴页~~
- [x] ~~B1 转移接口~~、~~B2 WebSocket~~、~~B5/B6 界面解耦~~、~~WebView2 彻底移除~~
- [x] ~~维护操作搬到网页~~、~~装备显示打磨~~、~~内置词缀表~~、~~前端部署自愈~~

---

## 悬而未决（需要使用者决策）

| 问题 | 现状 | 在哪 |
|---|---|---|
| ~~C# HTTP 服务用什么~~ | ✅ **已定：Kestrel** | `03-目标架构.md` §9 |
| ~~端口固定还是动态~~ | ✅ **已定：固定 3031，占用即报错** | 同上 |
| ~~是否需要认证 token~~ | ✅ **已定：暂不加**（仅监听 127.0.0.1） | 同上 |
| 状态管理方案（Context / Zustand） | 目前用 Context 够用 | 同上 |
| ~~视觉设计方向~~ | ✅ **已定：以游戏内实际观感为准、逐项校准** | 同上 |
| **旧前端 `WebUI/` 何时删** | 已被 `WebUI-next/` 取代，目录仍在，只作参考 | 同上 |

---

## 最近完成

| 日期 | 内容 |
|---|---|
| 2026-09-12 | **装备显示三轮（对着游戏校准）**：tooltip 配色逐档改成游戏实测值、分三级缩进、职业名按各职业主图分色；补齐游戏因"导出时角色职业不同"而缺颜色码的行。另：标签页图标、滚动条（照 grimtools 改）、设置/数据库页居中、详细对照禁用「详情」、修侧栏面板撑破页面导致的 hover 抖动 → [`12-装备显示与交互.md`](./12-装备显示与交互.md) |
| 2026-09-12 | **物品列表分页 + 两处居中**：工具条新增「加载」（无限滚动 / 翻页）与「每页」（50/100/200/500）两个下拉，偏好存 `localStorage`；翻页控件固定在列表**下方**（不随列表滚），无限滚动用 `IntersectionObserver` 在列表内续加载（实测 66 件：初始 50 → 自动续到 66）；设置页与数据库页内容改为水平居中。顺带实测出后端 `total` 在 `offset > 0` 时不可信（见「代码 / 环境」待办） |
| 2026-09-12 | **★ 搜索/过滤后端打通**：新增 `GET /api/filters/options`（品质/槽位/职业/比较符/分组复选框/属性下拉）；搜索框**按属性名匹配**（搜"火焰抗性"找出带该属性的物品）；修掉 Minimal API 字符串枚举导致的 400。属性数值过滤、槽位、品质、职业等**逐条实测通过** → [`09-高级搜索.md`](./09-高级搜索.md) |
| 2026-09-12 | **装备显示二轮**：品质色按指定取值；类型文本代替品质枚举名；**内置词缀表**（503 条，含四个 DLC）让名字不再依赖游戏汉化包；布局重构——**只有列表滚动**、详情支持**左/右固定栏** → [`12-装备显示与交互.md`](./12-装备显示与交互.md) |
| 2026-09-12 | **修：原版 IA 覆盖共用 storage** 导致界面变回旧样式；新增前端部署自愈 |
| 2026-09-12 | **装备显示首轮**：详情改渲染游戏原样 tooltip（15 种伤害/抗性配色）；技能名去英文、技能与职业名统一色；点空白取消选中；方形拟物化「取出」按钮；「详细对照」视图 |
| 2026-09-12 | **维护操作搬到网页**：`MaintenanceService` + `/api/maintenance/*` + 进度推送；「清除数据库」故意不搬；旧窗口全删 |
| 2026-09-12 | **界面解耦完成（B5/B6）**：WebView2 彻底移除；主窗口**永不显示**（修掉"启动一闪而过"） |
| 2026-09-12 | **新增 `11-备份与恢复.md`**：查清自动备份的周期/内容/命名、设置里那两项的真正含义（"压缩备份"其实是"额外备份到自定义目录"）、以及浏览器能不能弹窗选文件 |
| 2026-09-12 | **全量文档整理**：清掉"WebView2 代码还在 / 待 B5-B6 删"等已不成立的表述；`03` 端点表按实际重写；`AGENT.md` 通信图修正（原写"WebSocket 尚未实现"） |
| 2026-09-12 | **★ B2 完成**：`/ws` 推送 `itemsChanged`。两条路实测通过（改设置 / 把物品放进共享仓库，网页不用刷新自己就更新） |
| 2026-09-12 | **修好物品转移**：C# 补上 `POST /api/items/transfer`（此前只有 Node 原型有，前端一直 404）。双向实测通过 |
| 2026-09-12 | **设置页完工** + 删除图鉴页 + **新前端上线（B4）** |
| 2026-09-12 | **B1 实测通过**：`127.0.0.1:3031` 端点全通 |
| 2026-09-11 | **线 A 全部完成**（步 0–6）；环境搭建（.NET SDK、构建跑通）；确定「agent 与仓库留 WSL，Windows 只当构建靶机」 |

<details>
<summary>更早的完成记录（点击展开）</summary>

| 日期 | 内容 |
|---|---|
| 2026-09-11 | 新增 `tools/devapi/` 只读开发数据服务（**已退役**）；数据库备份到 `~/iagd-db-backup/` |
| 2026-09-11 | 属性翻译**简单层**（688 条真实属性）；新增 `08-属性翻译.md` |
| 2026-09-11 | 新增 `09-高级搜索.md`（grimtools 过滤器参考 + 命名同源发现） |
| 2026-09-11 | 完成全部文档重组（现状 / 目标 / 计划分离） |
| 2026-09-11 | 确定技术路线：后端服务化 + 浏览器 + React 重做，REST + WebSocket |

</details>

---

## 交接说明

### ★ 新对话怎么开始

只需要一句话：

> **"读 `AGENT.md` 和 `.docs/00-当前状态.md`"**

`AGENT.md` 是稳定的工程背景与工作方式；本文件是进度。**其余 `.docs/` 按需查阅**：

| 想做的事 | 该读 |
|---|---|
| 碰前端 | `04-开发环境.md`（环境与流程）+ `03-目标架构.md` §5、§6 |
| 碰后端 / C# | `03-目标架构.md` §4、§7 + `04-开发环境.md` §9 |
| 碰装备显示 / 详情面板 / 颜色 | `12-装备显示与交互.md` |
| 碰属性翻译 | `08-属性翻译.md` |
| 做搜索 / 过滤 | `09-高级搜索.md` + `03-目标架构.md` §4.5 |
| 碰备份 / 导入导出 / 文件选择 | `11-备份与恢复.md` |
| 动启动流程 / 托盘 / 界面结构 | `10-界面解耦.md` |
| 不确定历史决定 | `05-实施计划.md` 的决策记录 |

### 不要依赖聊天记录

**聊天记录会关闭/丢失，文档才是持久的。** 如果你发现某个信息"只在对话里说过"，
说明文档有缺口——**把它补进文档**，而不是靠摘要传递。

### 当前运行环境的要点（容易踩坑）

1. **agent 与仓库都在 WSL**，Windows 只当构建靶机 → `04-开发环境.md` §1
2. **Windows 侧验证 HTTP**：WSL 访问不到 Windows 的 `127.0.0.1:3031`，
   要用 PowerShell 发请求 → `04-开发环境.md` §9.4
3. **跑 Debug 还是 Release**：Debug 用隔离的 `userdata-test.db`（空库），
   验证真实数据要用 **Release** → `04-开发环境.md` §9.5
4. **不能从 UNC 路径运行 exe**：`\\wsl.localhost\...` 会**静默失败**，
   要复制到 Windows 本地再跑 → `04-开发环境.md` §9.6
5. **改前端后要部署**：把 `WebUI-next/build/` 拷进 `storage/`。
   注意**先删 `storage/assets`**，否则旧哈希文件会堆积。
   ⚠️ 部署后端前必须先停 IA（`IAGrim.exe` 在运行时会锁住 dll/exe）。
6. **不要打开 `C:\Program Files\IAGD\IAGrim.exe`**：它会把旧前端解压覆盖到
   共用的 `storage/`（见上方「一个必读的坑」）。我们的构建在
   `C:\Users\jyl96\iagd-release\`。

### 每次会话结束前

1. 让 AI 更新本文件的「现在在哪 / 下一步 / 最近完成」。
2. 把新产生的决定归档到 `.docs/05-实施计划.md` 的决策记录。
3. `git commit`——**提交信息本身就是"过程"的载体**，不必写进文档。
