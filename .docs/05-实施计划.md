# 05 · 实施计划

> 本文回答：**分几步走、每步做到什么算完、做过哪些决定。**
> 设计依据见 [`03-目标架构.md`](./03-目标架构.md)。

---

## 1. 总体策略：两条线并行

```
线 A：新前端（不需要后端、不需要 Windows，只需 Node.js）
  0 → 1 → 2 → 3 → 4 → 5 → 6

线 B：后端服务化（需要 Windows + .NET SDK）
  HTTP/WS 与旧路径并存 → 验证 → 删旧路径

线 C：界面迁移与收尾（依赖 A + B）
  WinForms 界面搬进网页 → 删掉外壳
```

**线 A 现在就能开始**（用假数据），线 B 需要 Windows 环境，两条线可以并行。

**原则**：小步快跑，每步都能跑、能看、能验证，每步都提交 git。

---

## 2. 线 A：新前端（增量步骤）

| 步 | 目标 | 验证什么 | 需要后端 | 状态 |
|---|---|---|---|---|
| **0** | 页面能显示物品（**已改为真实数据**，不再用假数据） | 工具链 + 通信层通了 | ✅ | ✅ |
| **1** | 显示一列真实物品（用真实 `IItem` 类型） | 渲染逻辑 | ✅ | ✅ |
| **2** | 从后端 HTTP 拿**真实**物品列表 | **通信层通了**（分水岭） | ✅ | ✅ 已由 A0/A1 顺带完成 |
| **3** | 搜索框 | 交互 → 后端查询 | ✅ | ✅ |
| **4** | 视图切换（分栏列表 / 简洁卡片） | **P0 核心目标** | ❌ | ✅ |
| **5** | hover 详情面板 | **P0 核心目标** | ❌ | ✅ |
| **6** | 转移物品（写操作） | 反向通信 | ✅ | ✅ 沙盒验证 |

> **与原始设计的一处偏离**：步 0 原定"硬编码一条假物品"，实际直接用了**真实数据**——
> 因为 `tools/devapi`（见 [`04-开发环境.md`](./04-开发环境.md) §8）已经可用，
> 用真数据能顺带验证通信层，少写一遍注定要删的假数据。

步 0、1、4、5 **只需 Node.js**（数据来自 `tools/devapi`，不依赖 Windows）。

### 每步的完成标准

- **步 0**：浏览器打开 `localhost:3000` 能看到一条物品信息（**真实数据**）。✅ 已完成
- **步 1**：能看到一列真实物品，数据形状与真实 `IItem` 一致。
- **步 2**：页面上显示的是**数据库里真实存在的物品**。
- **步 3**：输入关键词能过滤结果。
- **步 4**：能用选择框在两种以上视图间切换。
- **步 5**：hover 任意物品能看到完整属性，且能滚动。
- **步 6**：点击 Transfer 后，物品真的回到游戏共享箱。

---

## 3. 线 B：后端服务化

| 步 | 目标 | 说明 |
|---|---|---|
| B1 | 加 HTTP 服务（REST） | 监听 `127.0.0.1:3031`（Kestrel）✅ 已实测通过 |
| B2 | 加 WebSocket | `ws://127.0.0.1:3031/ws`，推送 `itemsChanged` ✅ 已实测通过（见 §6 的协议决定） |
| B3 | 前端切到新通信层 | ✅ 新前端本来就用 REST，不经 `hostObjects` |
| B4 | 启动时打开系统浏览器 | ✅ 并把主窗口收进托盘；托盘双击也打开 web UI |
| B5 | 删掉 WebView2 | ✅ **完成**（2026-09-12）：`CefBrowserHandler` / `JavascriptIntegration` / `IOMessage*` 协议 / `DarkMode` / `TooltipHelper` / `BackupLoginNagScreen` / `WebView2Runtime` 全删，csproj 移除包引用。构建输出 48 → 41 个文件 |
| B6 | 删掉 WinForms **界面** | ✅ **完成**（2026-09-12）：所有主界面在网页上（物品 / 设置 / 数据库）。`MainWindow` 仍在但**永不显示**（`SetVisibleCore` 拦截），只提供消息循环与 `Invoke`。按需弹出的对话框仍在 |

### 3.1 ★ B5/B6 的实质：解耦，不是删除（2026-09-12 起）

**`MainWindow` 同时干着两件不相干的事**，这是删不掉它的原因：

1. **界面宿主** —— WebView2、搜索框、标签页。**已经不需要了**
   （`storage/` 换了新前端，浏览器接管界面，主窗口启动几秒后就被 `Hide()`）。
2. **整个应用的服务装配点** —— `MainWindow_Load` 里创建了转移控制器、CSV 解析、
   replica parser、后台备份、注入器、各种定时器与事件订阅，还有 B2 的广播点。
   `MainWindow_FormClosing` 则是**退出清理**（顺序 Dispose 掉上面这一堆）。

所以 B5/B6 的内容是**把第 2 件搬到一个不依赖 UI 的宿主**，而不是删代码。

#### 不能真的"一个窗口都不留"

| 为什么还需要窗口 | 说明 |
|---|---|
| 注入器 | 要一个消息窗口收 `WM_COPYDATA`（`RegisterWindow("GDIAWindowClass", ...)`） |
| 托盘图标 | `NotifyIcon` 需要消息循环；`Application.Run` 也得有个宿主 |

所以目标形态是 **`ApplicationContext`**（不可见、不承载界面），暂称 **AppHost**：

```
Program.cs
  → 服务装配（本来就在 MainWindow 之前：ServiceProvider / ParsingService / 自动解析）
  → Application.Run(new AppHost(serviceProvider, parsingService))
        ├── NotifyIcon（托盘：打开 web UI / 退出）
        ├── WebServer（Kestrel，127.0.0.1:3031）
        ├── 注入器 + RegisterWindow 消息窗口
        ├── CSV 解析 / replica parser / 后台备份 / WebSocketSync
        ├── ItemTransferController
        └── 维护窗口（按需创建，不是主界面）
```

#### 已经确认的两点

- **`Program.cs:323` 的 `_mw.Visible = false` 是无效的**：`Application.Run(_mw)`
  会把它重新显示，真正隐藏要等 WebView2 初始化完成（**几秒**）后的
  `BeginInvoke(() => Hide())`。这就是"启动时旧界面一闪而过"的成因。
  AppHost 落地后它自然消失。
- **`SearchController` 解耦很干净**：`public IBrowserCallbacks? Browser` 是**可空**
  的，HTTP 用的 `QueryItems()` 不经过它。删掉 WebView2 只需要不再给它赋值；
  旧界面才走的 `Search(...)` 没人调用即可。
- `UpdatingPlayerItemsScreen` 的进度来自 `StatUpdateUIBackgroundWorker` 的
  `progress` 回调——**"更新项目统计"本来就有进度接口**，只是接在 WinForms
  进度条上。将来要推到网页不需要新造轮子。

**关键**：B1–B4 之间"与旧路径并存"的策略已经完成使命，并且**已经收尾**——
2026-09-12 起 `storage/` 换成了新前端，**浏览器接管界面**；
随后 B5/B6 把 WebView2 与 WinForms 界面**全部删除**。

> **当时的顺序（先并存、再切换）是有价值的**：切换前能先验证新通路，
> 出问题删掉调用即可退回。验证完之后删旧的这一步，施工记录与踩到的坑见
> [`10-界面解耦.md`](./10-界面解耦.md)。
>
> ⚠️ **但 B5/B6 不能现在做**：Settings / Mods 等功能目前**只在新前端里补了一部分**，
> 旧界面一删就没了入口。先把功能补齐（见线 C）再删。

---

## 4. 线 C：界面迁移

| 步 | 目标 | 难度 | 状态 |
|---|---|---|---|
| C1 | 搜索框搬进网页 | 中 | ✅ |
| C2 | 过滤器面板搬进网页（职业、技能、数值区间） | **高——工作量最大的一块** | ▶ **后端已完成**（2026-09-12），前端待做 |
| C3 | 设置页、Mod 页搬进网页 | 中 | ✅ |
| C4 | 清理旧前端代码 | 低 |

> ★ 2026-09-12：**C2 的后端部分已全部完成并实测**（`GET /api/filters/options`、
> 属性名搜索、数值过滤、槽位/品质/职业过滤）。
> 剩下的 C2 是**纯前端**：按 [`09-高级搜索.md`](./09-高级搜索.md) §3–§4 的信息架构做面板，
> 把勾选翻译成 `ItemSearchRequest`（类型已在 `WebUI-next/src/api/types.ts` 定义好）。

> C 线是**唯一工作量大且无法回避**的部分。它的进度决定整个改造何时能用。

---

## 5. 里程碑

| 里程碑 | 条件 | 意义 |
|---|---|---|
| **M1 能看到东西** | 线 A 步 0–1 | 工具链验证通过 ✅ |
| **M2 能查自己的装备** | 线 A 步 2–3 + 线 B B1 | 核心链路打通 ✅ |
| **M3 界面达标** | 线 A 步 4–5 | **P0 目标达成** ✅ |
| **M4 能取回装备** | 线 A 步 6 + 线 B B2–B3 | 功能完整 ✅ **2026-09-12 双向实测通过** |
| **M5 脱离 WinForms** | 线 B B4–B6 + 线 C | 项目目标达成 — **主体已达成**（主界面已全部在网页上）；剩 C2 过滤面板（**后端已完成，只差面板 UI**）+ 显示细节打磨 |

---

## 6. 决策记录

> 只记"决定了什么 + 为什么"。过程属于 git 提交信息。

| 日期 | 决策 | 理由 |
|---|---|---|
| 2026-09-11 | 后端功能逻辑不做修改，只改外壳与界面 | 降低风险，保证可回退 |
| 2026-09-11 | 本仓库定位为自用分支，不顾虑上游兼容 | 使用者本人 fork 自用，无协作约束 |
| 2026-09-11 | 技术路线选 **C：后端服务化 + 浏览器访问** | 浏览器 DevTools 让样式开发效率质变；不受 WinForms/WebView2 双重限制 |
| 2026-09-11 | 前端**重做**而非改造 | 技术路线变了，通信层与界面都要重写；改造反而更费事 |
| 2026-09-11 | 前端框架用 **React**（替代 Preact） | 教程与生态最丰富，初学者遇问题容易搜到答案 |
| 2026-09-11 | 通信选 **REST + WebSocket** | 请求-响应用 REST；后端主动推送用 WebSocket |
| 2026-09-11 | WebSocket **沿用现有消息枚举** | 只换传输层不改语义，C# 与前端的状态处理逻辑都能保留 |
| 2026-09-11 | 采用"视图切换"而非单一布局 | 不同场景需求不同：找装备适合列表，浏览适合卡片 |
| 2026-09-11 | 视图组件与数据逻辑分离 | 新增样式成本低 |
| 2026-09-11 | **排除**"仓库网格 / 自由摆放"样式 | 固定位置排列与**检索工具**的设计意图不符，且把整理负担推给用户 |
| 2026-09-11 | hover 详情：hover 看简版 + 点击固定看全版 | 现有 tooltip 装不下双绿 MI 的 20+ 条属性，且不可交互 |
| 2026-09-11 | 托盘图标保留 `NotifyIcon`，不换第三方库 | 最成熟稳定；"用 WinForms 一个控件"不等于"用 WinForms 做界面" |
| 2026-09-11 | grimtools **只借鉴设计与 CSS，代码不能直接抄** | 它是 jQuery + 服务端渲染，技术栈不同（见 `.docs/06-外部参考.md`） |
| 2026-09-11 | 不引入 CSS 框架，沿用 CSS 变量方案 | 现有变量体系设计得好，够用 |
| 2026-09-11 | 不要 Visual Studio，用 VS Code + C# Dev Kit | 命令行 `dotnet build` 足够；唯一缺的 WinForms 设计器恰好用不上 |
| 2026-09-11 | **运行方式：agent 留 WSL，仓库留 WSL，Windows 只当「构建靶机」** | agent 的工具链主路径在 Linux；工作量最大的界面迁移不依赖 Windows；跨边界只发生在编译，`npm install` 的数万小文件留在 WSL 原生文件系统 |
| 2026-09-12 | C# 的 HTTP 服务用 **Kestrel**（`FrameworkReference`，FDD 引用） | 路由/静态文件/中间件全是白拿的；实测**应用输出体积 0 增长**（48 文件 20 MB，一个 ASP.NET Core 程序集都没拷进来） |
| 2026-09-12 | 视觉以**游戏内实际观感**为准，逐项校准，而不是"看着像" | 猜的颜色经不起对照（绿装名字不绿、40 件传奇被画成蓝）。使用者能对着游戏给出准确色值，那就照抄 |
| 2026-09-12 | 职业名**按职业分色**，不跟人物职业走 | 库里的装备不属于任何人物；10 个色取自各职业的主图 |
| 2026-09-12 | 端口**固定 3031**，占用即报错 | 行为可预期，不做自动换端口 |
| 2026-09-12 | 认证 token **第一版不加** | 只监听 `127.0.0.1`；待分发或需要时再评估 |
| 2026-09-12 | **不再"新旧并存"，直接切换**（使用者决定） | 新前端已能跑（列表/搜索/详情/转移），并存只会拖着两套东西 |
| 2026-09-12 | **删除图鉴页**（前后端代码全删） | 使用者确认完全不需要 |
| 2026-09-12 | 设置项**按使用者逐项确认收窄**，只保留 4 项 + Stash | 暗色/最小化/自动更新等在浏览器形态下没意义；多电脑与在线备份依赖**原作者的服务器**，改为将来自建云备份；语言锁死中文 |
| 2026-09-12 | **重置设置改为热重置**（不重启程序） | 只把界面上能改的几项恢复初始值——原来"删文件+重启"是从旧界面沿用的，对改几个开关来说太重，且重启会断开浏览器里的界面 |
| 2026-09-12 | 重置时备份的是**设置文件**，不是物品数据 | 使用者的明确要求；备份落在 `settings-backups/` |
| 2026-09-12 | 两个后端有**数据形状差异**时，**以 C# 为准**，前端做兼容 | `ItemHtmlWriter` 是 WebView2 与 HTTP **共用**的，改它会波及旧界面（违反「功能行为不能坏」）→ `.docs/03-目标架构.md` §4.6 |
| 2026-09-12 | HTTP 转移**直接复用 `ItemTransferController`**，不另写一套 | 转移的语义（写 CSV 给 hook、再删记录）很微妙，重写必然与旧界面行为不一致 |
| 2026-09-12 | 开启「转移到任意 Mod」时，HTTP 转移**明确拒绝**而不是尝试 | 该分支要弹 `StashPicker` 模态框，必须跑在 UI 线程；HTTP 在线程池线程上，弹窗会直接抛异常。宁可报错也不让请求半路崩 |
| 2026-09-12 | B2（WebSocket）之前先用**轮询 total** 兜底刷新列表 | 不刷新就看不到新拾取的物品，是"能不能日常用"的问题；轮询只需比较一个数字，代价极小，且 B2 落地后可直接删除 |
| 2026-09-12 | B2 的 WS **只发轻量信号，数据仍走 REST**——**不沿用** `IOMessage` 枚举 | 原决策「沿用现有枚举」的前提已不存在：旧枚举是喂给 Preact 前端的，而新前端是重写的 React，**从来没消费过它**（连 `core.SignalReady()` 都不调），沿用省不下任何工作量，反而要把 `SetItems` 那种整份物品列表塞进推送。前端自己知道在看什么（搜索词、分页），发个"变了"由它自己重查更省 |
| 2026-09-12 | 广播点挂在 `CsvParsingService.OnItemLooted` 等**数据真正变化的地方**，不挂 UI 方法 | 一开始挂在 `ListviewUpdateTrigger` 上，实测发现那是**设置变更**用的，拾取走的是 `OnItemLooted`——挂错地方功能会静默失效。改对后实测：放进共享仓库，网页不刷新自己就更新了 |
| 2026-09-12 | 轮询降级**保留**，只在 WS 断开时启用（不是删掉） | 后端重启、浏览器断线时界面不能变成死数据；`live === false` 才轮询 |
| 2026-09-12 | **数据库 / Mods 页面不迁移**，永久留在 WinForms（使用者决定） | 它是低频维护工具（加载数据库在游戏更新时是自动的、配置只在 IA 找不到游戏时用一次、清除与更新统计是排障），不值得为它把 `ParsingService` 的解析流程解耦。**影响 B5/B6：不能"只留 NotifyIcon"，要保留这个窗口** |
| 2026-09-12 | 「清除数据库」**不搬到网页**（后端端点保留但无入口） | 使用者问「这个按钮是干什么的、不会把东西都删了吧」。查证：它只 DROP 并重建 `DatabaseItem_v2` / `DatabaseItemStat_v2` / `ItemTag` 三张**游戏数据**表，不碰 `PlayerItem`（物品不会丢），但会让界面变成"物品还在、没名字没属性"。而它做的事「加载数据库」第一步就做了（先 `Clean()` 再解析）——**没有它做不到的事，却能制造一个看起来像物品全没了的界面** |
| 2026-09-12 | **改主意：数据库 / Mods 页面还是搬到网页**，不留 WinForms 窗口 | 使用者问「在 web ui 里不能做到和这个窗口一样的事情吗」。复查后发现成本比原先估计的低得多：`ProgressTracker` **本来就与 UI 无关**，焊死的只是 `WinformsProgressBar` 那层 20 行适配器。搬完 WinForms 界面就真的一个不剩了 |
| 2026-09-12 | 「配置」在网页上改为**粘贴路径** + 后端校验 | 浏览器出于安全**不允许**网页打开原生文件夹选择器。硬要保留那个对话框的话，就得为它留一整个 WinForms 依赖 |
| 2026-09-12 | 计数式的 `EnterMaintenance` 必须配「状态真的变了」的判断 | 实测踩到：`OnStateChanged` 每次进度更新都触发，把计数推到几十，`ExitMaintenance` 永远减不回 0 → **界面永久 503**、看起来像物品全丢了。用 `_maintenanceActive` 记住上次的值，只在变化时进出 |
| 2026-09-12 | 加载数据库期间**冻结 web UI**（而不是任其查询一个被清空的库） | `buttonForceUpdate_Click` 第一件事就是 `Clean()` 清空游戏数据库，之后是几分钟的解析。这期间网页查询会拿到空数据/不完整数据。改为：WS 广播 `maintenance` 状态、HTTP 查询端点返回 503、前端显示遮罩 |
| 2026-09-12 | 详情**渲染游戏原样 tooltip**（`replicaStats`），不从 `DatabaseItemStat` 重拼 | 游戏已经把它生成的完整 tooltip 原样给了 IA（带颜色代码、套装、授予技能、转换行）。实测这批物品的 `headerStats` **全是空的**、`bodyStats` 最多 4 条，重拼既费劲又不可能对齐 |
| 2026-09-12 | 颜色代码的 **RGB 值自己定**（`--tt-*`） | 代码→属性类型的对应是汉化包给的（`tags_ui.txt`），但**具体色值没有公开来源**——硬编码在游戏的 `gameengine.dbr` 里（Crate 论坛确认）。目标是"看起来像"，不是逐像素一致；日后挖到真值只需换变量 |
| 2026-09-12 | 技能与职业名**统一颜色**、英文技能名**不显示**、"需要"**不用红色** | 游戏那三处行为都依赖"当前人物"（按职业灰掉非本职技能、红色表示属性不够），而库里的装备不属于任何人物。见 `12-装备显示与交互.md` §1.2 |
| 2026-09-12 | **自己维护词缀表**，物品名由前端组装 | 原来靠"游戏当前汉化包 + 重新加载数据库"。使用者要求固定下来：`model/affixes.json`（503 条，含四个 DLC 文件）。任一侧词缀查不到就**整体回退**到 C# 拼的名字，宁可两个都用游戏文本也不要缺一半 |
| 2026-09-12 | 名字用**类型文本**（"传奇双手锤"）而不是品质枚举名（Epic/Blue） | 枚举名是数据库内部值，对使用者没意义；游戏原版的类型文本既说清品质也说清部位，而且它就在 tooltip 的 `type=66` 行里，不用自己拼 |
| 2026-09-12 | 布局改成**固定高度 flex 列**，只有物品列表滚动 | 使用者的原话是"滑动的时候只在这个框内滑动，表头不动"。⚠️ flex 列里 `.app` 与 `.app__content` 都必须 `min-height: 0`，否则内部滚动整个失效 |
| 2026-09-12 | 详情固定栏用 **grid 的列**而不是 `position: fixed` | 这样它天然位于工具条下方（"不顶头"），且栏宽由**模式**决定、**与是否选中无关**（"始终留空"） |
| 2026-09-12 | 前端**自带一份产物 + 启动时同步**到 storage | 原版 IA（`C:\Program Files\IAGD`）与我们共用用户数据目录，它启动时会用旧前端覆盖 `storage/`。自愈不能阻止覆盖，但能保证我们的程序一起来就修好。⚠️ 只碰前端文件，绝不动物品图标 |
| 2026-09-12 | **属性数值过滤不另写**，只补"可选项接口" | `PlayerItemDaoImpl` 里 `Filters`（属性存在性）与 `StatValueFilters`（数值，走 `ComputedItemStat` 预计算表）**早就实现**，旧界面一直在用；新前端缺的只是可选项与 UI。重写必然与旧界面行为不一致（违反「功能行为不能坏」） |
| 2026-09-12 | **过滤器定义搬到后端**（`Services/Filters/FilterCatalog.cs`），照搬旧面板 4 组 | 前端不再抄一份"勾了什么对应哪些 stat 字段"的映射。两份映射迟早漂移，而"勾了火焰抗性却按别的字段过滤"这种错几乎不可能被发现 |
| 2026-09-12 | 槽位清单**写死在 `SlotTranslator`**，不查数据库 | 实测 `DatabaseItemStat_v2` 里 `stat='Class'` 有 **175** 个值，混着容器、可破坏物、怪物、药水等非装备类别；旧界面（`UIHelper.SlotFilter`）用的也是这份固定清单 |
| 2026-09-12 | `filters/options` 的属性下拉**只列已预计算**的属性 | `statValueFilters` 直接查 `ComputedItemStat`；把没算过的属性列出来，使用者会选到一个永远查不到东西的条件 |
| 2026-09-12 | 搜索框的关键词**同时匹配属性名**（与名字是 OR） | 使用者 2026-09-11 的要求：「搜火焰抗性要找出带该属性的物品」。解析用两个来源——固定组的人工标签（伤害类属性没有同名模板，只能靠它）+ 语言表的同名模板 |
| 2026-09-12 | 关键词→属性的模板来源**排除 `customtag_*` / `iatag_*` / `tag*`** | 那些是组合模板（如 `{1} {3} 伤害`），key 不是数据库里的 stat 名，去掉占位符只剩"伤害"两个字，会把使用者引到一堆不存在的字段上 |
| 2026-09-12 | Minimal API **打开字符串枚举**（`ConfigureHttpJsonOptions`） | System.Text.Json 默认只认数字枚举，`"operator":"GreaterOrEqual"` 会返回 **400 且响应体为空**（实测很难查）；让前端传 `0..4` 太易错。只影响请求绑定，响应仍是 Newtonsoft |
| 2026-09-12 | **修掉 `offset > 0` 时 `total` 不准**：DAO 的零成本捷径补 `skip == 0`，`QueryItems` 只在翻页时付一次 COUNT | "这一页没满"**只在第一页**能推出总数。旧界面路径本来就不受影响（跨页游标 `_dbSkip` 起步就是 `MaxSearchResults`，首页不截断时根本不翻页），是**新前端的 offset 分页**暴露了这个隐含假设。实测 66 件 / 每页 50，第 2、3 页的 total 都正确 |
| 2026-09-12 | 品质**多选**另开 `Rarities` 字段，**不动**单值的 `Rarity` | 过滤器面板的稀有度是 checklist（`09-高级搜索.md` §3 的设计），单值表达不了；但 `Rarity` 是旧界面路径在用的，改它就会破坏「功能行为不能坏」。多选优先、单值兜底，两条路互不干扰 |
| 2026-09-12 | 排序做成 `ItemSearchRequest.OrderByLevel`（**进 DTO**），而不是给 `QueryItems` 加参数 | HTTP 两条通路（`/api/items`、`/api/search`）都从请求体拿条件，放进 DTO 它们自动都能用；`QueryItems` 的签名也不用动。它只影响排序、不进 `IsEmpty` |
| 2026-09-12 | **Mod 相关功能暂缓**（使用者决定），但把查清的东西写成独立文档 | 它牵动四处（数据出口 + 工具条 + 分页语义 + 转移通路），是横切改动；而 Mod 的现状（`Mod` 是"结果集前提"、旧下拉的数据源是 `GetModSelection()` 而非"已安装 mod"）很容易被忘掉或搞错 → [`13-后端待办与计划.md`](./13-后端待办与计划.md) §3 |
| 2026-09-12 | 界面拆成**两块**：折叠的过滤面板管"勾选类"，高级搜索弹层管"输入类" | 两者的交互本来就不同——勾选适合常驻可视、输入（数值区间/多条条件）适合弹出来慢慢填。硬塞进一块会让面板要么太长、要么总挡着列表 → [`09-高级搜索.md`](./09-高级搜索.md) §7 |
| 2026-09-12 | 过滤选项做成**圆角色块**（未选阳刻 / 选中阴刻），「清除」做成**直角矩形** | 使用者要求。直角与圆角并置是有意的：清除是"破坏性的一次性动作"，不该和"可反复切换的选项"长得一样 |
| 2026-09-12 | 前端**品质按值去重**，不做"1/2 个绿色词缀"细分 | 多选的 `rarities` 是数组、`prefixRarity` 是单值，两者不兼容；硬凑会得到"稀有（不限词缀）OR 1 个词缀"这种没意义的结果 → [`14-疑难决定.md`](./14-疑难决定.md) §2 |
| 2026-09-12 | 死代码清理的判据：**删掉之后会不会有人回来找它** | "零引用"不等于"该删"。逻辑已被取代或功能已明确不做的删掉；只是"暂时没入口、将来要用"的（如 `ImportExport/`，备份恢复的现成实现）保留 → [`14-疑难决定.md`](./14-疑难决定.md) §3 |
| 2026-09-12 | 修 `StatManager` 的**前缀变体**缺口 + 两个**用反的模板** | 缺口在 C# 侧同样成立（白名单 + 无兜底，取不到模板就静默丢掉）。顺带发现"减少目标伤害"与"降低目标抗性"的模板互相换了——那是写错，不是设计 → [`14-疑难决定.md`](./14-疑难决定.md) §5 |
| 2026-09-12 | `headerStats` 全为空是**有意的抑制**，不修 | `ItemHtmlWriter` 在有游戏 tooltip（`replicaStats`）时主动清空它，避免同一件装备渲染两遍。真正该做的是"列表要不要关键属性列"，而那个要先定"哪几个算关键属性" |
| 2026-09-13 | 过滤器用**颜色表达逻辑**：黄 = 与、绿 = 或。逻辑挂在**组**上——点组名切换整组，组里的选项跟着变色；四个属性组与**职业**可切，品质/槽位无切换（天生是"或"），开关类无切换（独立布尔，只能是"与"） | 使用者要的是"点『直接伤害』这种组名时颜色切换，后面 chip 之间的逻辑改为与/或"。同组属性本来就该共用一个逻辑，组级切换既符合直觉又不必给每个选项加开关。物品属性那一侧后端本来就有"组间与、组内或"的 `filters`；职业那一侧原来只有"与"，补了 `classesAny` → [`14-疑难决定.md`](./14-疑难决定.md) §8 |
| 2026-09-13 | 新增 `rarityConditions`（品质 + 词缀数，组内或）来支持**双稀有** | `rarities` 只表达品质值，而"双稀有"= `Green` **且** 词缀数 ≥ 2；单值的 `prefixRarity` 与多选不兼容。做成后端认识的一条条件后，"双稀有 或 传奇"这类组合是免费的 → §9 |
| 2026-09-13 | "所有物品"用**反选**（排除全部装备槽位）实现，不列"非装备槽位"清单 | 槽位清单是人整理的，列"非装备"只会重演同一份可能不完备的分类；反选才真能兜住遗漏 → §10 |
| 2026-09-13 | 槽位的**分组/顺序/改名**与十个**基础职业**硬编码在 `FilterCatalog` | 那些是使用者的选择，既非游戏数据顺序也非翻译表能推导；而且"哪几个槽位算装备"从此只有一个来源，"所有物品"的反选正好用它 → §12 |
| 2026-09-13 | 高级搜索做成**居中模态**、内容与过滤面板**重复但共享状态** | grimtools 的高级搜索就是完整面板；只放"面板没有的"会让"想一处看全"无处可去。共享状态保证不存在两套条件 → §13 |
| 2026-09-13 | **排序**从高级搜索搬到**视图工具条**，并升级成四级：入库时间（默认）/ 品质 / 等级 / 名称 | 排序是"怎么看"而非"看什么"：每次都要开模态改太绕，也不该被"清除过滤条件"清掉或计入"已选 N 项"。次序由后端 `BuildOrderBy` 拼 SQL——分页切片在后端，前端重排会让"第几页"对不上。品质必须用 `CASE Rarity` 映射权重（库里的 `Epic` 其实是传奇）→ [`14-疑难决定.md`](./14-疑难决定.md) §15 |
| 2026-09-13 | 高级搜索改**草稿态**：打开时复制当前条件，点「完成」才生效，「取消」丢弃；顶部补上自己的搜索框（关键词同样走草稿） | 使用者要求"高级搜索中的选项只用于筛选最终搜索结果，而非实时影响当前显示"。高级搜索是"想清楚再执行"，过滤面板才是"边点边看"——两种节奏不强行统一 → §14 |
| 2026-09-13 | **字体 / 字号倍率 / 快捷键**存浏览器 localStorage，**不进** `settings.json` | 它们纯粹是浏览器观感，后端既不读也不用；进 C# 设置要动 DTO + 存储 + 序列化，收益为零。与 `iagd.filters` / `iagd.sortBy` 同类 → §16 |
| 2026-09-13 | **专注模式**下 header / 页签 / 工具条 / 过滤面板**不渲染**（而不是 CSS 隐藏）；`f` 弹出"过滤器 + 视图工具条"对话框 | 少一层"看得见但点不到"的状态，也不会留下能被快捷键聚焦的隐藏输入框；工具条组件两处复用 → §17 |
| 2026-09-13 | 高级搜索的选项是**搜索条件**，不是过滤器：新增独立的 `useSearchConditions`，与 `useFilters` 在请求里合并（AND）；加「退出搜索」 | 使用者的心智模型："勾穿刺伤害 = 搜「某某装备 AND 穿刺伤害」"，点「完成」不该改动过滤器配置。两者生命周期不同（搜索是一轮、过滤器可长期挂着），混成一份的话「退出搜索」就得猜要不要连过滤器一起清 → §18 |
| 2026-09-13 | 快捷键改键冲突时**两项互换**，而不是拒绝 | 键位总数固定，想把某个键腾出来通常就是"我要换到那个键上"，交换一步到位；拒绝要先改对方再改自己，两步且中间态可能又撞上 → §19 |
| 2026-09-13 | `sortBy: 'level'` 改成**等级降序** | 使用者指定（其余三个方向不变：入库时间新→旧、品质从好到差、名称升序）→ §20 |
| 2026-09-13 | 过滤器「杂项」里那个 `setbonus` 的中文标签从**"属性"改成"套装"** | 后端早就支持套装筛选（存在性过滤 `setName` / `itemSetName`），是 `zh.txt` 里的翻译把它叫成了"属性"，界面上根本看不出那是套装。只改翻译、不加新字段 |
| 2026-09-13 | 套装授予技能用**文本形状**区分名字 / 说明 / 参数 | 那一块整块都是 `type 82`（普通授予技能才有 `37` 名字 / `39` 说明），type 里没有可用信息；形状在实测数据上很干净 → §21 |
| 2026-09-13 | 套装加成（`type 28`）**不做**颜色补全，整块保持默认色 | 游戏导出时那些行就没有颜色码，旁边却夹着会被补色的"技能等级 +3"，整块花白不一。使用者："如果全都是白色的那还好"——一致性优先 → §22 |
| 2026-09-14 | **详情方式按视图区分**：分栏列表只留左/右固定栏，简洁卡片才保留浮动 | 分栏列表是逐行扫视的表格，浮动面板跟在鼠标旁会一直挡住相邻行；简洁卡片是网格、卡片大小不一，浮动预览正好合适。偏好不被当前视图支持时**自动回退但不写回存储**，切回去原选择还在 |
| 2026-09-14 | 浮动面板**摆在卡片另一侧**，垂直与卡片**中线对齐** | 使用者要求"根据卡片在排列中的左右位置显示在另一侧""在可以完全显示的情况下和卡片居中（中线）对齐"。面板通常比卡片高，所以居中要用**实测高度**（绘制前量），且必须保证完整可见 |
| 2026-09-14 | 分栏列表**限宽居中**：名称 ≤25 汉字 / 类型 ≤8 汉字 / 两列 ≥4 汉字，超长省略号截断 | 使用者给了具体字数上限。表格因此有 `min-width`——再窄就出**横向滚动条**，而不是把列压到看不清；固定栏也在极窄（≤750px）时让位 |
| 2026-09-14 | 简洁卡片删「品质」标签 /「来源」/「硬核」/ `baseRecord`，图标改 `object-fit: contain` | 「来源」在库里只有 `Player` 一种（好友 / 图纸不进浏览器列表）；「硬核」是账号维度、列表本来就按当前角色过滤过；`baseRecord` 是内部记录名。图标被拉伸是写死 64×64 导致，与「详细对照」统一用 `contain` |
| 2026-09-14 | `^R` 火焰改用**正红 `#ff3b30`** | 使用者指定"接近大红、比纯 `#f00` 好看、有火焰感"。深色底对比度 ≈4.65，与柔和的流血 `#e35a5a` 仍能分开 |
| 2026-09-14 | 品质排序的**次关键字等级也改降序** | 使用者报"选品质时等级还是从低到高，反了"。品质只是第一关键字，同一品质内应与「等级」排序一致（高等级在前） |
| 2026-09-14 | `skillRowKind()` 的 `<职业>` 判定**限定行首** | 「神话 撒旦」的触发效果行 `造成暴击时有 25% 几率触发<爆破>手榴弹…` 也含职业标记，但它是**参数行**；纯技能名去码后一定以 `<` 开头 |
| 2026-09-14 | **与原版共存：调研，不改**（新增 [`15`](./15-与原版%20IAGD%20共存.md)） | 同时运行被同一个单实例互斥体 GUID 挡住（且我们窗口永不显示，原版看起来"毫无反应"）；真正共存要改**数据目录 + 互斥体 + 端口**三处。使用者决定先不做 |
| 2026-09-14 | **布局按视图独立**：宽屏"列表居中 + 对侧留空"只给分栏列表；简洁卡片是"固定栏占一侧、另一侧卡片铺满" | 使用者澄清"简洁卡片和分栏视图的固定栏是完全分开的……每一种视图都是独立的样式调整"。宽屏那条规则因此带 `data-view='table'`，不做成一套通用列宽 |
| 2026-09-14 | 详情抬头删 `item-type`；图标**外框一律 64×64，只有首饰（勋章 / 项链 / 戒指）显示 32×32** | 抬头的类型文本与属性区 `tt-type-66` 完全重复；首饰图标实测就是 32×32 小方图（`storage/*.tex.png` 里 1109 个），拉到 64 会糊——其他种类"顶格"（`object-fit: contain`），也顺带修掉非正方形图标的拉伸 |
| 2026-09-14 | 详情面板与「详细对照」卡片**合并成一个 `TooltipCard` 组件**；字体设置**从两套并成一套** | 两者显示的本来就是同一个 tooltip（游戏原样导出），只是一个放侧栏 / 浮动、一个并排铺开。原先各写一套抬头与属性区，字号（11 vs 12）、内边距、图标处理已经开始漂移。共用后"字体、字号、颜色"天然一致；只有 `item-detail__close` 与「取出」按钮不同，用 `action` 插槽传入。旧存档的 `compareFont*` / `detailFont*` 仍会被读取 |
| 2026-09-14 | **物品名 14px、等级 12px 四处统一，并一起跟字号倍率缩放**；分栏列表的名字从 12px 提到 14px | 使用者："把所有地方的标题和等级都统一成完全一样的大小……列表里面的名字现在也是小一号的。" 倍率若只作用于属性区，调大后名字反而更显小，所以名字 / 等级 / 属性区共用同一个倍率。⚠️ 表格列宽用 `em`，倍率因此写在 `.item-table` 的 `font-size` 上，列宽随之等比缩放 |
| 2026-09-14 | 分栏列表：类型列按**最长的类型文本**预留宽度（10 个汉字 → `width: 12em`）；表格 `max-width` 50em→**54em**；单元格左右内边距 10px→**8px** | 使用者报"放到最大都显示不全『传奇双手远程武器』"，并预计最长是"双稀有双手远程武器"。查库实测 type 66 行最长 **8 字**（"史诗双手远程武器"），按 10 字预留更保险。空间从单元格内边距里挤出来——使用者明确"`app__list` 宽度可以不变，把里面的内容往左右两边拉一下" |
| 2026-09-14 | 滚动容器（`.app__list` / `.tt-card__body`）拆成**「外壳 + 滚动层」两层**，遮罩条钉在外壳上：上下各一条 **6px** 的背景色长条（`::before`/`::after` + `absolute`） | 使用者："滚动条出现时在顶部底部有一个和背景颜色一样、4 像素高的长条，字消失的地方离边框就有 4 像素。" **内边距做不到**（内边距属于滚动内容）。⚠️ 第一版把遮罩条做成滚动层自己的 `sticky` 伪元素，贴不到边——sticky 的粘性约束矩形是父元素的 content box，越不过 padding；拆两层后 `absolute` 钉在不滚动的那层，才真正贴住边框。条高由使用者随后定为 **6px** |
| 2026-09-14 | 详细对照新增**两种卡片布局**（它的「详情」下拉从置灰变为可选）：`full` **全部显示** = 瀑布流（CSS 多列）、`fixed-height` **固定高度** = **上限** = 内容区高度 × 0.85（`ResizeObserver` 跟着 `.app__content`，窗口 / 过滤器开合都同步） | 使用者要求。瀑布流用 CSS 多列而不是 JS 量高度：不需要 JS、`break-inside: avoid` 天然防拆列。⚠️ 多列是**纵向**填充（先填满一列再下一列），不是从左到右逐行。固定高度下属性多的卡片**内部滚动**（带上下遮罩条） |
| 2026-09-14 | **「详情」偏好按视图各存一份**（localStorage 里按视图 id 存 map；旧的全局字符串当作 `'*'` 默认值） | 使用者："不同视图下详情的选择各不相同。" 原来只存一个全局值、切视图时靠"收敛"回退，会把 A 视图的选择带到 B 视图里去 |
| 2026-09-15 | 背景分三层：页面 `--bg` #1c1a17 → 列表 / 卡片区 `--bg-sunken` **#131109** → 卡片与面板 `--bg-panel` **#322c25** | 使用者："他们背景颜色和后面的背景颜色是一样的……把他们的背景颜色略微提亮一点，后面的背景颜色压暗一点，让他们能明显地分开。" 原来 `--bg-panel` 一个色同时当卡片和卡片所在的容器，详细对照的卡片、简洁卡片的卡片全糊在列表里；详情面板 `#1f1c18` 也和页面几乎同色。⚠️ 遮罩条与吸顶表头必须跟 `--bg-sunken`，`.item-detail` 的 `--tt-mask-bg` 必须跟 `--bg-panel` |
| 2026-09-15 | 「固定高度」明确是**上限**：一行都装得下时，行高取**该行最高的那张卡**（不硬撑到 0.85、不在下面留大片空白） | 使用者补充："如果一行里面所有的卡片本身的高度都小于设置的最大高度，那么这一行的高度就直接按照这一行里卡片的最大高度算。" 实现只要两条 CSS：flex 的**默认 `stretch`**（行内等高）+ 卡片的 `max-height` ⇒ `行高 = min(该行最高卡片内容高度, 上限)`，不需要 JS 分行或量高 |

---

## 7. 风险与注意事项

| 风险 | 说明 | 应对 |
|---|---|---|
| **C2 过滤器面板迁移** | 工作量最大，且涉及职业/技能/数值区间等复杂逻辑 | ✅ **后端风险已消除**（2026-09-12 实测通过）；剩下的纯前端可按既定信息架构（`09-高级搜索.md` §3–§4）分块做 |
| **数据丢失** | 物品数据库是多年积累 | 任何涉及数据库的改动先备份；不改动数据库结构 |
| **物品转移的即时性** | 曾是"改 HTTP 后要变异步"的顾虑 | ✅ **已结论：保持同步**。转移是写 CSV 交给游戏 hook，请求内就能返回成功/失败；实测双向都通，前端不需要中间状态。**仍需要一个刷新才看到列表变化**（B2 未做前靠轮询兜底） |
| **程序生命周期** | 关了浏览器程序是否继续运行 | 托盘常驻；网页提供退出入口或只用托盘 |
| **WebSocket 断线** | 长时间运行可能断开 | 前端实现自动重连 + 重连后重新同步 |
| **性能** | 列表可能上千件，hover 面板频繁创建销毁会卡 | 复用面板实例；必要时做虚拟滚动 |
| **旧代码清理** | 两套前端并存期间容易混淆 | 新结构稳定后一次性删干净，不要长期并存 |

---

## 8. 环境前提

> 工作方式见 [`04-开发环境.md`](./04-开发环境.md)：
> **agent 与仓库都在 WSL，Windows 只当「构建靶机」。**

| 阶段 | 能做什么 | 环境要求 |
|---|---|---|
| **现在** | 读代码、写文档、做设计 | ✅ 已满足 |
| **现在** | **线 A**（步 0、1、4、5 用假数据；步 2、3、6 需后端） | ✅ `npm install` 已完成，`npm run dev` 即可 |
| **现在** | **线 B + 线 C**（后端服务化、界面迁移） | ✅ .NET SDK 10.0.401 已装，`dotnet build` 已跑通（0 错误） |

详见 [`04-开发环境.md`](./04-开发环境.md)。