## Todo List

### 前端

 ^R 为正红色 （接近大红色，但是要好看一点，有火焰的感觉的颜色，网上查查设计师给的相关配色）



#### 视图 - 详细对照

`compare-card__head` 的 `item-type` 删去

神话 撒旦 - `造成暴击时有 25% 几率触发<爆破>手榴弹 (Grenado) 冷却时间减少 2 秒` /  `造成暴击时有 50% 几率触发<爆破>罐式炸弹 (Cansiter Bomb) 冷却时间减少 2 秒` 。 这两个是效果被错误识别成了 `tt-type-82 tt-indent-2 tt-skill-name` 。改回正确颜色即可。



#### 视图 - 分栏列表

`<tr> ` 没有和 `app__list` 的顶端对齐

`<tr> ` 的等级/类型应该“居中”

`<tr> ` 的槽位删去

分栏列表应该以合适的宽度居中，详情 - 浮动 删去，只留左侧固定栏/右侧固定栏两种类型。

当页面宽度足够时，列表居中，固定栏紧贴在一侧，另一侧留空。当页面宽度不足时，列表和固定栏占据全部宽度，列表不居中，另一侧不留空。

表格的名称最宽 25 个汉字（50个字符宽度），类型最宽容纳8个汉字（16个字符宽度）

表格的名称和类型最窄容纳4个汉字（8个字符宽度）

在极窄时固定栏消失（小于固定栏宽度+表格出现下侧scrollbar时宽度）。



#### 视图 - 简洁卡片

浮动`class="item-detail"` `data-mode="hover"`里的 `item-detail__foot` 没有存在的必要

item-card__meta：

​	品质 删去，只留后面的 item-type 就够了

​	详情 - 浮动 删去，只留左侧固定栏/右侧固定栏

​	等级和后面的数字没有对齐，目前数字的位置偏上

​	来源 玩家 -- 删去，来源是从哪儿获得的数据？有哪些类型？

​	硬核 否 -- 删去，是否硬核影响什么东西吗

item-card__record 删去

贴图 -- 目前的装备图片 `item-card__icon` 都被拉伸了，他的样式应该像详情对照里面的图片 `compare-card__icon` 一样，保持比例不变

在极窄时不显示浮动 `item-detail` 或固定栏（大概在750px）

浮动 - 选中时显示的 `class="item-detail"` `data-mode="hover"` 根据点击的卡片在排列中的左右位置显示在另一侧

浮动 - 悬浮时显示的  `class="item-detail"` `data-mode="hover"` 要灵活安排位置 1. 显示更多内容 2. 在可以完全显示的情况下和卡片居中（中线）对齐



### 后端

排序选择 品质 时，等级依旧是从低到高，反了。

现在的程序和老的 iagd 是不能共存的吗？就算不同时打开，老的 iagd 的也无法正常打开？最好能够共存。（调研答问题，产生文档说明。先不做）









