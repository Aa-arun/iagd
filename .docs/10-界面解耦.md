# 10 · 界面解耦（B5/B6 的施工图）

> 本文回答：**怎么把 `MainWindow` 去掉，而不丢掉功能。**
> 背景与动机见 [`05-实施计划.md`](./05-实施计划.md) §3.1；进度见 [`00-当前状态.md`](./00-当前状态.md)。

**建立于**：2026-09-12（依据一次对 `MainWindow.cs` 的全量只读审计）

---

## 1. 为什么不能直接删

`MainWindow` 同时干着两件不相干的事：

1. **界面宿主** —— WebView2、搜索框、设置页、标签页。**已经不需要**
   （`storage/` 换了新前端，浏览器接管界面）。
2. **整个应用的服务装配点与消息循环宿主** —— `MainWindow_Load`（610–887 行，约 66 项）
   + `MainWindow_FormClosing`（368–422 行，19 项 `Dispose`）。

所以 B5/B6 的实质是**搬家**。

### 一个反直觉的好消息

**引用 `MainWindow` 这个类型的，全仓库只有两处**：

| 位置 | 用途 |
|---|---|
| `Program.cs:24/39/302/318/331` | 构造、`Visible=false`、`Application.Run(_mw)`、静态属性 |
| `LanguagePackPicker.cs:124` | `Program.MainWindow?.UpdateLanguage()`（模态框关闭后刷新整窗语言） |

其余全是**注释里提到**（`ModsDatabaseConfig.cs:40`、`WebServer.cs:111/263`）。
`StartupService.cs` 完全不引用它。`IAGrim/Http/` 下只有 `WebServer.cs` 的注释提到，
没有代码级类型引用。

**这意味着删窗口不难，难的是搬装配。**

---

## 2. ★ 关键决策：隐藏的宿主 Form，而不是 `ApplicationContext`

曾考虑用 `ApplicationContext` 做宿主，但审计发现**三处硬要求 Form/Control**：

| 组件 | 要求 |
|---|---|
| `MinimizeToTrayHandler`(`MinimizeToTrayHandler.cs:18`) | 构造签名就是 `(Form, NotifyIcon, ...)`，还订阅 `Form.SizeChanged`、读写 `WindowState`/`Hide()` |
| `WindowSizeManager`(`MainWindow.cs:822`) | 要求 `Form`，订阅 `FormClosing` |
| `DarkMode`(`MainWindow.cs:745`) | 要求一个 `Control` 当 root |
| `CustomWndProc`(`MainWindow.cs:429-466`) | 用 `MainWindow.Invoke` 从注入线程切回 UI 线程 |

**结论：保留一个不可见的宿主 Form**，`Application.Run(host)` 照旧。
这样上面四样**一行都不用改**，`ApplicationContext` 方案省下的复杂度远小于它带来的改造量。

宿主只需要：`Visible = false`（并用 `SetVisibleCore` 阻止被显示）、一个 `NotifyIcon`、
一个 `Invoke` 能力。**不需要任何 Designer 控件。**

---

## 3. 必须保留的 WinForms 能力（最小集）

| 能力 | 谁需要 | 说明 |
|---|---|---|
| 消息循环 | 全部 | `Application.Run` |
| 隐藏的 `Invoke` 目标 | 注入器回调、B2 广播 | 从注入线程切回 |
| `NotifyIcon` + 右键菜单 | 托盘 | 打开 web UI / 打开维护窗口 / 退出 |
| Win32 消息窗口 | 注入器 | `RegisterWindow`（`DllInjector/RegisterWindow.cs`，**不是 Form**，自己 `CreateWindowExW`，只需消息循环） |
| 可显示维护窗口 | `ModsDatabaseConfig` | 见 [`00`](./00-当前状态.md) 的「维护操作的拆分」 |

> `RegisterWindow` / `InjectionHelper` **本身无需改动**。

---

## 4. 搬迁清单（按依赖顺序）

### 组 1：先做，且独立 —— HTTP 服务不再挂在 WebView2 上

**现状（最危险的一处）**：`MainWindow.cs:288-296` 在
`Browser_CoreWebView2InitializationCompleted` 回调里 `new WebServer(...)` + `Start()` +
`OpenWebUi()`。**删掉 WebView2，HTTP 服务会一起消失。**

搬到宿主启动流程，并把 `() => _transferController` 这个工厂
（`MainWindow.cs:294` → `WebServer.cs:114/263/270`）改成从宿主的字段取。

### 组 2：纯后台服务（UI 依赖 = 无，可直接照抄）

`_authService`(676)、`backupService`/`_charBackupService`/`_backupServiceWorker`(709-711)、
`_webSocketSyncService`(716-717)、`_buddyItemsService`(789-795)、`_backupBackgroundTask`(798)、
`_itemReplicaService`(763)、`_itemStatPrecomputeService`(766)、
`_csvParsingService` + 两个 `CsvFileMonitor`(844-848, 878-880)、
`_itemReplicaParser` + 预载线程(850-853, 883)、`cacher`(639)、
`JsIntegration` 的后端事件订阅(632, 636, 718-727, 729, 821)。

### 组 3：B2 广播点解耦（6 处，**每处都同时耦合了 WinForms 对象**）

| 位置 | 依赖的 WinForms 东西 |
|---|---|
| `MainWindow.cs:590` `ListviewUpdateTrigger()` | `_searchWindow` |
| `MainWindow.cs:607` `DatabaseLoadedTrigger()` | `_searchWindow`、`_darkMode`、`_itemReplicaService` |
| `MainWindow.cs:869` `OnItemLooted` 闭包 | `_searchWindow`（**这是"捡到东西"的主路径**） |
| `MainWindow.cs:649-650` 解析开始/结束 | 仅 `_webServer` 字段 |
| `MainWindow.cs:701-704` maintenance 回调 | 仅 `_webServer` 字段 |

搬到宿主后直接订阅 `ParsingService` / `CsvParsingService` 的事件，`WebServer` 直接注入。
**同时要处理旁边对 `_searchWindow` 的调用**（586/594/603/858/861）。

### 组 4：注入器链路

`StartInjector`(900-923)、`CustomWndProc`(429-466)、`WineMessagePollTick`(934-992)、
`_messageProcessors`(802-803)、`InjectorCallback`(105-192) 及字段清理(403-408, 415-416)。

对 `MainWindow` 的依赖只有 4 点：
1. `Shown` 提供的启动时机 → 改成宿主启动后直接调用
2. 消息循环 → 宿主提供
3. `Invoke` 切线程 → 宿主提供
4. `_searchWindow?.ModSelectionHandler.UpdateModSelection`(452/460) → **需要找替代**，
   这是唯一真正的界面依赖
5. 写 `statusLabel.Text`(127/158) → 改走 `UserFeedbackService`

### 组 5：`CefBrowserHandler` 的三个接口角色

它同时实现三个接口，被广泛注入（`StashPicker`、`StashTabPicker`、`AddEditBuddy`、
`ModsDatabaseConfig`、`OnlineSettings`、`ItemTransferController`、`BackupService`…）：

| 接口 | 方法数 | 处置 |
|---|---|---|
| `IHelpService` | 4 | ✅ **已完成**：换用现成的 `IAGrim/Services/HelpService.cs`（`Process.Start` 打开系统浏览器）。`MainWindow._helpService` |
| `IUserFeedbackHandler` | **1**（`ShowMessage`） | ✅ **已完成**：新增 `IAGrim/Services/WebUiFeedbackHandler.cs`，推 WS `notification` 给页面（前端弹出提示）。⚠️ 不能沿用旧实现的 `\n`/`'` 转义——那是拼 JS 源码用的，走 JSON 会让页面看到字面的 `\n` |
| `IBrowserCallbacks` | **9** | 全是旧界面推送，删引用（待 P6） |

> `UserFeedbackService` 与 `ItemTransferController` 的构造参数都从具体的
> `CefBrowserHandler` 换成了接口，所以它们不再需要 WebView2 活着。
> **这一步必须排在搬迁之前**：`TransferStashService` / `CsvParsingService` 等
> 后台服务都经由 `UserFeedbackService` 间接依赖反馈通道。

### 组 6：托盘与退出

- `exitToolStripMenuItem_Click`(1012) 现在靠"关闭窗口"退出 → 宿主需要明确的退出路径
- `MainWindow_FormClosing`(368-422) 的 19 项 `Dispose` 搬到宿主的统一 `Dispose/Exit`
  ⚠️ 其中**漏了 `_webServer.Dispose()`**（`WebServer.cs` 有实现，但退出时没被调用）
- `Program.MainWindow` 静态属性（`Program.cs:39`）唯一消费者是 `LanguagePackPicker.cs:124`

---

## 5. 界面各自的归宿

| 界面 | 归宿 |
|---|---|
| `SplitSearchWindow`（671 行 + Designer） | **删**（网页取代） |
| WebView2 + `tabControl1` | **删** |
| `SettingsWindow` / `OnlineSettings` | 网页设置页已取代 → 删 |
| `ModsDatabaseConfig` | **永久留 WinForms**，但改成独立窗口（不再嵌 Panel） |
| `LanguagePackPicker` | 语言锁死中文，可删（待确认） |
| `DarkMode` | 随主界面删除（网页自己有暗色变量） |
| `NotifyIcon` + 托盘菜单 | **留**，移进宿主 |

---

## 6. 分阶段计划（每阶段都能编译、能跑、能验证）

| 阶段 | 内容 | 验证 |
|---|---|---|
| **P1** ✅ | 组 1：HTTP 服务移出 WebView2 回调 | 已完成（2026-09-12）：`MainWindow.StartWebServer()` 由 `MainWindow_Load` 末尾调用。实测 HTTP 就绪 4.2s，早于 WebView2 回调（相差 37ms——本机 WebView2 初始化很快，所以**时间差不明显**；解耦的实际价值在于 **WebView2 初始化失败时 HTTP 服务仍然可用**） |
| **P2** | 建 `HostWindow`（隐藏 Form）+ 组 2 纯后台服务搬迁 | 程序能启动、托盘在、注入器工作、拾取能入库 |
| **P3** | 组 3：B2 广播点解耦 | 网页仍能自动刷新；维护遮罩仍工作 |
| **P4** | 组 4 + 组 6：注入器与退出清理 | 游戏注入正常；退出进程干净（无残留进程） |
| **P5** | 组 5：`CefBrowserHandler` 换实现 + 删 WebView2 | 转移/提示仍工作；WebView2 引用清零 |
| **P6** | 删 `SplitSearchWindow` / `SettingsWindow` / 主窗口本体 | 全功能回归 |
| **P7** | 维护窗口独立化 + 托盘菜单入口 | 四个维护操作可达 |

> **P1 有一个遗留**：启动时的自动解析发生在 `Program.cs:268-287`，
> 早于 `MainWindow` 构造，所以那时 `WebServer` 还不存在——解析期间网页**连不上**
> （而不是看到维护遮罩）。要修得把 `WebServer` 提到 `Program.cs`，
> 但它的转移工厂依赖 `_transferController`，而后者依赖 `CefBrowserHandler`
> 作 feedback。**所以这件事必须等组 5（P5）换掉 `CefBrowserHandler` 之后**。

---

## 7. 坑（审计中发现的，踩之前先看）

1. **HTTP 服务的生命周期挂在 WebView2 回调里** —— 这是 P1 必须先做的原因。
2. **`Program.cs:318` 的 `_mw.Visible = false` 是无效的**：`Application.Run(_mw)`
   会重新显示它，真正隐藏要等 WebView2 初始化完成（**几秒**）后的 `BeginInvoke(Hide())`。
   这就是"启动时旧界面一闪而过"的成因。
3. **每个 B2 广播点都耦合了 WinForms 对象**，没有一个是纯后端事件。
4. **`CustomWndProc` 里的 `_searchWindow.ModSelectionHandler`** 是注入链路唯一的真界面依赖。
5. **退出清理漏了 `_webServer.Dispose()`**。
6. **启动时的自动解析发生在 `MainWindow` 创建之前**（`Program.cs:268-287`），
   那时 `WebServer` 还不存在 → 解析期间网页**连不上**（而不是看到维护遮罩）。
   宿主提前后应该顺带修好：让 HTTP 服务在自动解析之前就起来。
7. `DarkMode` 以 `MainWindow` 自身当 root（`DarkMode.cs:30`），删窗口时注意它的 `Dispose`。
8. `MainWindow.NumInstantSyncItemCount`(`MainWindow.cs:79`) 是**死字段**，仓库内无引用。
