# 13 · 后端待办与计划

> 本文件回答：**后端还剩什么没做、为什么、按什么顺序做。**
> 它是 [`00-当前状态.md`](./00-当前状态.md) 里「下一步」的**展开**——00 只留一行结论，细节在这里。
>
> ⚠️ 这不是进度看板（进度仍看 `00`）。它是"**尚未做**的事"的清单：做完一项就把它从这里删掉，
> 在 `05-实施计划.md` 的决策记录里留一条"为什么这么做"。
>
> 建立于 **2026-09-12**（搜索/过滤后端打通之后）。

---

## 1. 现状：后端已经具备什么

| 领域 | 状态 |
|---|---|
| HTTP + WebSocket | ✅ 唯一通路（`127.0.0.1:3031` + `/ws` 轻量信号），WebView2 与 WinForms 界面已删 |
| 物品检索 | ✅ 名字 / **属性名** / 属性存在性 / 属性数值 / 槽位 / **品质（含多选）** / 等级 / 职业 / 镶嵌 / 重复 / 战宠 / **排序** |
| 过滤器选项接口 | ✅ `GET /api/filters/options`（品质 / 槽位 / 职业 / 比较符 / 分组复选框 / 属性下拉） |
| 物品转移 | ✅ `POST /api/items/transfer`（双向实测通过）。⚠️ 一处**明确拒绝**，见 §2.2 |
| 维护操作 | ✅ 加载数据库 / 配置 / 重算属性，进度走 WS，期间查询 503 |
| 设置 | ✅ 读写 + 重置（热）+ 导入 / 导出。⚠️ 导入仍重启，见 §2.2 |
| 备份 | ✅ 自动备份照常生成。⚠️ **恢复没有入口**，见 §2.2 |
| 属性翻译 | ✅ 显示走 C# 的 `ItemStatService`（真正的 `StatManager`），前端渲染 `replicaStats` |

细节见 [`03-目标架构.md`](./03-目标架构.md) §4（协议）与 [`09-高级搜索.md`](./09-高级搜索.md)（搜索/过滤）。

---

## 2. 待办

### 2.1 与 C2 过滤面板配套（后端侧）

| # | 项 | 状态 |
|---|---|---|
| 1 | **品质多选** —— `ItemSearchRequest.Rarities`（`List<string>`），SQL 用 `IN`；单值的 `Rarity` 保留给旧界面路径 | ✅ 2026-09-12 实测：`Blue + Epic` → 63 件，四项 → 66 件（= 全部） |
| 2 | **排序** —— `ItemSearchRequest.OrderByLevel`，`QueryItems` 透传给 DAO | ✅ 2026-09-12 实测：等级升序 58 → 65 → 75 |
| 3 | **Mod 过滤的选择器数据** | ⏸ **暂不做**（使用者 2026-09-12 决定）→ 见 §3 |

> **C2 面板在**后端**这一侧已经不缺东西了。** 剩下的全在前端（按 `09-高级搜索.md` §3–§4 的信息架构做）。
> 前端可直接用的字段见 `03-目标架构.md` §4.5；类型定义在 `WebUI-next/src/api/types.ts`。

### 2.2 其它功能缺口

按"使用者会不会立刻撞上"排序：

| # | 项 | 现状与要点 |
|---|---|---|
| 4 | **「转移到任意 Mod」在浏览器上被拒绝** | 开启该设置时 HTTP 转移直接返回错误——因为 `ItemTransferController` 那条分支要弹 `StashPicker` **模态框**（必须 UI 线程）。见 §3.4 的第 3 步 |
| 5 | **备份恢复没有入口** | 备份一直在生成（含角色存档 + `export.ias`），但能读回它们的窗口随旧界面删除后**已无入口**。结论与做法全在 [`11-备份与恢复.md`](./11-备份与恢复.md)。使用者已决定暂缓 |
| 6 | **WebSocket 有广播盲区** | 改动**已有**物品（属性重算、改名）不触发 `itemsChanged`，界面不会自己刷新 |
| 7 | **预计算滞后** | 刚拾取的物品要等后台循环（每 2.5 秒 25 件）算完，**属性数值过滤才查得到它**。可在拾取时优先算那几件 |
| 8 | **`filters/options` 没有缓存** | 每次调用跑两次库查询（19,352 行的 `ItemTag` LIKE + `ComputedItemStat` DISTINCT）。低频，暂不值得做；真要做，得同时想好"重新加载数据库 / 预计算推进"后怎么失效 |
| 9 | **「导入设置」仍会重启程序** | 需要 `SettingsService` 支持逐字段写回（热重载）。**等使用者决策**（见 `00` 的待办） |
| 10 | **`headerStats` 实测全为空** | 待查是不是解析侧的问题（目前靠 `replicaStats` 兜住，功能上不缺） |

### 2.3 死代码清理（都已核实过引用关系）

| # | 死代码 | 核实结论 |
|---|---|---|
| 11 | **`IAGrim/UI/Filters/`（5 个 UserControl，约 2000 行）+ `IAGrim/UI/DesiredSkills.cs`** | `DesiredSkills` **除自己的 Designer 外零引用**；`Filters/*` 只被它引用 → **整块是死的**。它的**逻辑已原样搬进** `Services/Filters/FilterCatalog.cs`，删掉不丢能力（丢的只是旧面板 UI）<br>连带：`FirefoxCheckBox` 的数值过滤小按钮、`NumericFilterDialog` 会一起失去入口 |
| 12 | **`SearchController` 的整条旧界面通路** | `Browser` 字段**从未被赋值**，所以 `Search()` 两个重载、`ApplyItems()`、`UpdateCollectionItems()`、`AddBuddyItems()`、`OnSearch` 事件都是死代码<br>连带：`ItemCollectionDao`（图鉴已删）、`ItemPaginationService`、`_lastQuery` |
| 13 | **`MainWindow` → `AppHost` 改名** + Designer 里空的 TabControl / Panel | 它现在只是个不可见宿主（消息循环 + `Invoke`），名字还在骗人 |

> ⚠️ 删 11 / 12 之前先确认 `IBrowserCallbacks` 的**其余**用途还在（`WebUiBrowserCallbacks` 走 WS 给前端推提示、
> `ItemReplicaParser` / `BackupService` 在用）——要删的只是 `SearchController` 里那个**从未被赋值**的字段。

### 2.4 需要先核实的

| # | 项 | 说明 |
|---|---|---|
| 14 | **`00` 里那两条"属性翻译"待办可能已经过时** | 「特殊层」（技能修饰、伤害转换、种族加成）与「模板 key 前缀变体」写的是 **`tools/devapi` 的"简单层"** 缺口。但新前端渲染的是 **C# 生成的 `replicaStats`**，走的是真正的 `StatManager`（含那 28 个 `Process*` 方法）→ 那两条大概率不成立<br>⚠️ 唯一不确定的是"**前缀变体**（如 `customtag_xpac_modif_skillManaCostReduction`）在 C# 侧会不会也漏"。要下结论得拿一件带该属性的物品，比较游戏内 tooltip 与 IA 的 `replicaStats` |

---

## 3. ★ Mod 支持（暂缓）

> **决定（使用者，2026-09-12）：Mod 相关功能先不做。** 本节的目的是**别把已经查清的东西丢掉**。

### 3.1 现状：Mod 不是"过滤选项"，而是"结果集前提"

| 事实 | 说明 |
|---|---|
| `Mod` 的语义 | `PlayerItemDaoImpl.SearchForItems` 里：**空 = 只看非 Mod 物品**（`PI.Mod IS NULL OR PI.Mod = ''`），非空 = `LOWER(PI.Mod) = LOWER(:mod)`。**不是**"不过滤" |
| 硬核同理 | `PI.IsHardcore` / `NOT PI.IsHardcore` 二选一，也不是可选过滤 |
| 旧界面怎么用的 | 工具条上一个下拉，数据来自 **`IPlayerItemDao.GetModSelection()`**：`SELECT DISTINCT IFNULL(mod,''), ishardcore FROM playeritem` —— 注意它是 **"mod + 是否硬核"的组合**，因为两者都是前提 |
| ⚠️ 别搞混 | `GET /api/grimdawn/mods`（`MaintenanceService.GetMods`）返回的是**已安装的 mod**（扫游戏目录），用途是「加载数据库」时选哪个 mod 来解析。**它不能拿来当过滤下拉** |
| 新前端现状 | **没有任何 mod / 硬核选择器** → 界面上只能看到**非 Mod 物品**。玩 mod 的人等于看不到自己的东西 |

后端**数据是现成的**（`GetModSelection()` 一直在），缺的只是把它交出去 + 前端 UI + 转移那一段。

### 3.2 为什么暂缓

不是难度问题，是**改动面**问题——它同时牵动四块：

1. **数据出口**：要新开一个端点（或并入 `/api/filters/options`）返回 `{ mod, isHardcore }` 组合
2. **前端工具条**：加"仓库"选择器；⚠️ 切 mod 是**换前提**，必须重置分页并重查总数（不像普通过滤那样能就地翻页）
3. **硬核**：`isHardcore` 目前也是个前端没暴露的裸布尔。做 mod 时通常要**一起**做"普通 / 硬核"切换，否则硬核玩家的物品照样看不到
4. **转移**：要做到"任意 mod 转移"，得先把 §3.4 的第 3 步做掉

### 3.3 将来要做的三件事

| 步 | 内容 |
|---|---|
| 1 | **后端**：加 `GET /api/filters/mod-selection`（或并入 options），直接复用 `IPlayerItemDao.GetModSelection()`，返回 `[{ mod, isHardcore }]` |
| 2 | **前端**：工具条加"仓库"选择器（非 Mod / 各 Mod，含普通·硬核），切换时**重置分页**；把选中的 `mod` + `isHardcore` 放进 `ItemSearchRequest` |
| 3 | **转移**：把"选目标 mod"从**模态框**改成**数据**——后端先给可选目标列表 → 前端选 → 转移请求带上目标；`ItemTransferController` 加一条"目标已指定"的分支，绕开 `StashPicker`。做完这条，§2.2 第 4 项的拒绝就可以撤掉 |

### 3.4 验收标准

- 选某个 mod 后列表只剩该 mod 的物品，`total` 正确，翻页也正确
- 切"普通 / 硬核"能正确切换，且两者互不串
- 切回"非 Mod"能恢复原状
- 开启 `TransferAnyMod` 后，**从浏览器转移不再报错**，物品进到指定 mod 的仓库

---

## 4. 执行顺序

```
① C2 过滤面板的前端主体（后端已就绪）
        ↓  面板做出来才知道还缺什么
② 死代码清理（§2.3）——为读代码的人减负，与 ① 无依赖，可随时插入
        ↓
③ 功能缺口按"使用者会不会立刻撞上"补：
     WS 广播盲区 → 预计算滞后 → filters/options 缓存
        ↓
④ 使用者决策后再动：导入设置热重载、备份恢复入口
        ↓
⑤ Mod 支持（§3）——等前面都稳了再做，它牵动面最大
```

> **为什么不先做 Mod**：它会一次性改动"数据出口 + 工具条 + 分页语义 + 转移通路"四处，
> 而前面几项都是局部改动。先做局部、把界面用起来，再动这种横切的功能。
